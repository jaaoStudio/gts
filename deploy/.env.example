# 複製成 ~/gts-web/.env（VM 上），填入實際值
# Harbor（沿用 boss-timing 那組）
REMOTE_REGISTRY_IP=harbor.example.com

# 藍綠雙 slot 的目前 image tag（初次設定填 latest，之後由 deploy.sh 自動維護）
BLUE_TAG=latest
GREEN_TAG=latest

# 目前對外流量的 tag（deploy.sh 維護，僅供人看/追蹤）
ACTIVE_TAG=latest
