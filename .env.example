# Directus 後端設定
# 複製此檔案並重新命名為 .env，然後填入實際的網址
VITE_DIRECTUS_URL=https://your-directus-backend.example.com

# GA4 評估 ID。本機請留空 —— 填了就會把 dev 流量送進正式站報表。
# 正式站的值由 CI 於 build 時注入（deploy.yml 的 build-args）。
VITE_GA_ID=
